# main.rs

Two lines, so the whole plugin is testable in-crate. The binary is named
`nu_plugin_quest` rather than for its crate because nushell discovers
plugins by that filename prefix; the crate keeps the tool's own name.
