# tui.rs

EMPTY ON PURPOSE, for the same reason as `cli.rs`.

This is the camp-class surface. Camp currently needs nothing beyond `all`,
which is the evidence that `all` is carrying its job rather than an accident -
the one built consumer specializes not at all.
