# srvc.rs

EMPTY ON PURPOSE, for the same reason as `cli.rs`.

This is the dquest-class surface, and dquest is the eventual SOLE
implementation consumer serving inference. So this module is the likeliest of
the three to gain content, and it is where a serving-side specialization
belongs rather than in `all`.
