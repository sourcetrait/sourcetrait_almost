# main.rs

Thin by convention: the moment it would want a `use` statement, the work
belongs in `run.rs` instead.

The binary is named `bquest` while the crate is `sourcetrait_bquest_two`, so
the path is spelled out in the manifest rather than derived.
