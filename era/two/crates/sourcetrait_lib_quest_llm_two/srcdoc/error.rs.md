# error.rs

## enum LibQuestError

Name kept from the pre-split lib so the moved engine bodies and every
engine consumer compile unchanged; the harness lib's own error renamed
(HarnessQuestError) instead.
